from setuptools import setup

setup(name='NumberGuessGame',
        version='0.1',
        description='Guessing game with customization',
        packages=['NumberGuessGame'],
        zip_safe=False)
